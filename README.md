# Skilléa 🔁

Plateforme d'échange de compétences entre particuliers.

## Structure du projet

```
skillea/
├── public/
│   └── index.html        ← page HTML de base
├── src/
│   ├── index.js          ← point d'entrée React
│   └── App.jsx           ← tout le code du site
├── package.json          ← dépendances
└── vercel.json           ← config déploiement
```

## Déployer sur Vercel (sans terminal)

1. Va sur https://github.com et crée un compte
2. Crée un nouveau dépôt appelé "skillea"
3. Upload TOUS les fichiers de ce dossier (en gardant la structure)
4. Va sur https://vercel.com → "Add New Project"
5. Connecte ton GitHub → sélectionne "skillea"
6. Clique "Deploy" → c'est tout !
